# HANDS-001 自己レビュー

## SampleAppとの構成対応

- [ ] `feature/sample/presentation/common`を作成している
- [ ] `SampleGraph.kt`をcommonへ配置している
- [ ] `SampleRoutes.kt`をcommonへ配置している
- [ ] `SampleNavigationComponents.kt`をcommonへ配置している
- [ ] Home / History / Settingsを画面ディレクトリへ分けている
- [ ] Legal / License / VersionInfoのRouteをSample側へコピーしていない

## 責務

- [ ] AppNavHostはFeature Graphの登録中心になっている
- [ ] SampleGraphはSample Routeの登録を担当している
- [ ] RouteがNavControllerとScreenを接続している
- [ ] Screenは表示と操作通知だけを担当している
- [ ] SampleNavigationComponentsは表示とcallback通知だけを担当している
- [ ] ScreenがNavControllerを参照していない
- [ ] ViewModel / UseCase / Repositoryを不要に追加していない

## 動作

- [ ] Homeが初期表示される
- [ ] Bottom Navigationで3画面を切り替えられる
- [ ] Homeが選択状態になる
- [ ] Drawerを開閉できる
- [ ] Drawerの4項目からPlaceholderへ遷移できる
- [ ] Drawer表示中のBackでDrawerが閉じる
- [ ] 同じTabの連続選択でRouteが重複しない
- [ ] NavigationのBackが意図した順序で動作する

## UI / Resource

- [ ] 文言をResourceから取得している
- [ ] Material3 Themeを利用している
- [ ] 各PlaceholderのPreviewを確認できる
- [ ] 小さい端末でもレイアウトが崩れない

## 実行確認

- [ ] `./gradlew.bat :app:assembleDebug`
- [ ] `./gradlew.bat test`
- [ ] 実機またはエミュレーターで手動確認

## 振り返り

```text
自分が選択した構成:

迷った点:

設計書で確認した箇所:

SampleAppと比較して異なる点:

次のHome課題で追加する責務:
```
