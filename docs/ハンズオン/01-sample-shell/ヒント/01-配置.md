# Hint 01: 配置

SampleAppでは、Home / History / SettingsをSample Featureとしてまとめている。

```text
feature/sample/presentation/common/
  SampleGraph.kt
  SampleRoutes.kt
  SampleNavigationComponents.kt
```

画面固有のPlaceholderは`home`、`history`、`settings`へ分ける。
Drawer遷移先のRouteをSample側へコピーしない。
