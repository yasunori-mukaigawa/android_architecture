# Hint 03: Back Stack

Bottom Navigationでは、次の観点からNavigation APIの設定を考える。

- 同じRouteを重複させない
- startDestinationまで戻れる
- 必要ならタブごとの状態を復元する
- Drawerを閉じてから遷移する

選択中のBottom Navigation itemは、手動で別Stateへコピーするより、現在のRouteから導出できないか考える。
