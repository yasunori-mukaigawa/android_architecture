# Hint 02: 責務

次の責務を混ぜない。

```text
AppNavHost
  -> Feature Graphを登録

SampleGraph
  -> Sample Routeを登録

Route
  -> NavController、Drawer、State、Screenを接続

Screen
  -> 表示と操作callback

SampleNavigationComponents
  -> 表示とクリック通知
```

共通Composableが直接Navigationすると、後でRoute / Screen分離を崩しやすい。
