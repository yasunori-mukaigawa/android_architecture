# HANDS-002 回答チェックポイント

## 回答ブランチ

```text
solution/02-core-foundation-presentation
```

このStepではCore実装を作り直さない。回答側では、01のSolutionを起点に不要なStarterを整理し、既存CoreがSampleAppと一致していることを確認する。

## 回答Commitの想定

```text
01: Template由来のStarterを削除
02: Core契約の確認結果と学習補足を追加
03: Step別Diffを追加
```

Commit名や分割は実際の回答ブランチの内容に合わせてよい。ただし、各Commitはビルド可能な状態にする。

## 確認コマンド

```powershell
.\gradlew.bat :core:foundation:test
.\gradlew.bat :core:presentation:test
.\gradlew.bat :app:compileDebugKotlin
```

## SampleApp比較

比較対象は、Template側の次の実装とSampleApp側の対応ファイルとする。

```text
core/foundation/src/main/java
core/presentation/src/main/java
```

KDocや学習用コメントは追加してよいが、コメント以外の実装差分は残さない。
