# HANDS-003 回答チェックポイント

以下を自分の言葉で説明できることを確認する。

1. `StringProvider`をViewModelへ直接渡すのではなく、DIと契約を通して利用する理由は何か
2. `DispatcherProvider`と`CoroutineScopeProvider`は何を差し替え可能にするか
3. `AppLogger`とTimberの実装を分ける理由は何か
4. `RouteArgumentEncoder`だけをCoreへ置き、NavHostやNavControllerを置かない理由は何か
5. `AppDialog`とDialogを表示する判断を、なぜ同じ場所に実装しないのか
6. `BaseAppTheme`、`BaseThemePreset`、`BaseThemeRegistry`の役割は何か
7. TemplateのDefault ThemeとSampleAppのTheme設定の差分は、どのStepで解消するか
8. Templateに存在しないSampleAppの機能を、なぜ03ですべて実装しないのか

回答には、少なくとも次の図を含める。

```text
ProviderまたはUI部品
    -> Coreの契約
    -> Android / Compose実装
    -> DIまたはTheme接続
    -> App / Featureでの利用
```
